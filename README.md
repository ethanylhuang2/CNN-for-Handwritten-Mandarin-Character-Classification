# CNN for Handwritten Mandarin Character Classification

## Overview
Trained on the CASIA-HWDB Dataset
Images are loaded into dataset and prepreocessed
CNN is trained on dataset, used to predict class of image
